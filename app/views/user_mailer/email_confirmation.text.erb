Hi <%= @user.name.presence || @user.email %>,

A request was made to change your email address to <%= @user.pending_email %>.

Click the link below to confirm this change. The link expires in 24 hours.

<%= @confirmation_url %>

If you did not request this change, you can safely ignore this email.
Your current email address will remain active.
